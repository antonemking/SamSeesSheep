# Copy this to .env and fill in local-only overrides if needed.
# .env is gitignored — never commit real keys or pod connection details.

# No required root-level environment variables are currently needed.
#
# Optional, only for experiments/sheep-triage-jev. Export it in the shell.
# Never commit a real key.
# TYPESAFE_API_KEY=
