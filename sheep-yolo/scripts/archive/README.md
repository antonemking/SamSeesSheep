# Archived benchmark scripts

These scripts are superseded historical entry points kept for provenance while the canonical benchmark remains `../bench_held_out.py`.

Do not add new benchmark work here unless you are preserving an old run exactly as executed.
